<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.project_lupin.dao.PaymentDAO">

    <select id="payListbyId" resultType="com.boot.project_lupin.dto.PaymentDTO">
        SELECT
            b.buy_id,
            b.user_id,
            b.auction_id,
            b.bid_id,
            b.buy_date,
            b.buy_state,
            m.bid_money,
            m.bid_time
        FROM
            buy_tb b
        JOIN bid_tb m ON b.bid_id = m.bid_id
        WHERE
            b.user_id = #{user_id}
        ORDER BY b.buy_id DESC
    </select>

    <!-- 결제 정보를 buy_tb에 삽입 -->
    <insert id="insertPayment" parameterType="com.boot.project_lupin.dto.PaymentDTO">
        INSERT INTO buy_tb (buy_id, user_id, auction_id, bid_id, buy_date, buy_state)
        VALUES (#{buy_id}, #{user_id}, #{auction_id}, #{bid_id}, #{buy_date}, #{buy_state})
    </insert>

    <!-- bid_id를 사용하여 결제 금액을 조회 -->
    <select id="getMoneyByBidId" resultType="com.boot.project_lupin.dto.PaymentDTO">
        SELECT bid_money
        FROM bid_tb
        WHERE bid_id = #{bid_id}
    </select>
</mapper>
